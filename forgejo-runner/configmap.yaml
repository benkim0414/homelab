---
apiVersion: v1
kind: ConfigMap
metadata:
  name: forgejo-runner-config
  namespace: forgejo-runner
data:
  config.yml: |
    log:
      level: info
    runner:
      file: /data/.runner
      capacity: 2
      timeout: 3h
    cache:
      enabled: true
      dir: /data/cache
    container:
      network: host
      privileged: false
      force_pull: false
      docker_host: "tcp://localhost:2375"
