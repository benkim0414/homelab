apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: honcho-backups
  namespace: honcho
spec:
  storageClassName: longhorn
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 20Gi
