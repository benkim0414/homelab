sealed-secrets/sealed-secrets-master-keys.yaml
immich-backup.dump

# Honcho JWT (contains a long-lived bearer token — never commit)
honcho/hermes-jwt.txt

# Claude Code local settings (machine-specific overrides)
.claude/settings.local.json

# Session memory buffer (local-only, not for version control)
.remember/

# Local linked worktrees
.worktrees/

# mise local overrides (machine-specific)
.mise.local.toml
